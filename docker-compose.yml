version: '3.8'

services:
  web-frontend:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    container_name: web-frontend
    ports:
      - "3030:3030"
    environment:
      - NODE_ENV=development
    volumes:
      - .:/app
      - /app/node_modules
    networks:
      - dl-mobile_deuceleague  # Use the same network as dl-mobile

  # nginx:
  #   build: ./nginx
  #   ports:
  #     - "81:80"
  #   restart: always
  #   volumes:
  #     - ./nginx:/etc/nginx/conf.d
  #   networks:
  #     - deuceleague

networks:
  dl-mobile_deuceleague:
    external: true
    name: dl-mobile_deuceleague  # Explicitly specify the external network name
